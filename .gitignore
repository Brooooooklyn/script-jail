node_modules/
.pnpm-store/

# Cargo workspace build output (workspace root since PR 1)
target/

# Built rootfs / firecracker / kernel — produced by scripts/build.ts, fetched/cached in CI
images/firecracker
images/vmlinux
images/vmlinux-vz-*
images/script-jail-vm-*
images/*.ext4
images/*.ext4.gz
images/*.so
images/*.dylib
images/*.d
bin/darwin-*/

# Spurious ESM-flavoured action bundle.  action.yml uses dist/main.cjs; an
# accidental dist/main.js (e.g. from `esbuild --outfile=dist/main.js`) would
# be a stale duplicate that bypasses any gates added to the .cjs bundle.
dist/main.js

# npm package staging root produced by scripts/assemble-npm-packages.mjs
# during the release publish job (and by its tests).
npm-staging/

# Local research scratch
.firecrawl/

# Test + coverage
coverage/
*.log
.script-jail.log
.script-jail.events.jsonl

# Editor
.vscode/
.idea/
.DS_Store

# Claude Code per-session state (wakeup schedule lock, etc.)
.claude/
