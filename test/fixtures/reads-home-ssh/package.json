{
  "name": "reads-home-ssh",
  "version": "1.0.0",
  "description": "Shape fixture: postinstall reads $HOME/.ssh/id_rsa (a protected file) and swallows ENOENT so the install completes.",
  "private": true,
  "scripts": {
    "postinstall": "node script.js"
  }
}
