{
  "name": "tries-dlopen",
  "version": "1.0.0",
  "description": "Shape fixture: postinstall calls process.dlopen and should not be blocked by the default sandbox.",
  "private": true,
  "scripts": {
    "postinstall": "node script.js"
  }
}
